{% extends 'base.html.twig' %}

{% block body %}
    <twig:Header title="Accounts">
        <div class="sm:flex">
            <div class="flex items-center ml-auto space-x-2 sm:space-x-3">
                <a
                    class="px-3 py-2 text-xs font-medium text-center inline-flex items-center text-white bg-[#112D4E] rounded-lg hover:bg-[#112D4E]/90 focus:outline-none dark:bg-blue-600 dark:hover:bg-blue-700"
                    href="{{ path('app_settings_account_step1') }}"
                >
                    <svg class="w-3 h-3 text-white me-2" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 24 24">
                        <path fill-rule="evenodd" d="M2 12C2 6.477 6.477 2 12 2s10 4.477 10 10-4.477 10-10 10S2 17.523 2 12Zm11-4.243a1 1 0 1 0-2 0V11H7.757a1 1 0 1 0 0 2H11v3.243a1 1 0 1 0 2 0V13h3.243a1 1 0 1 0 0-2H13V7.757Z" clip-rule="evenodd"/>
                    </svg>
                    Account-Wizzard
                </a>
            </div>
        </div>
    </twig:Header>

    <twig:Content>
        <table
            class="min-w-full divide-y divide-gray-200 table-fixed dark:divide-gray-600"
            data-controller="account-table"
        >
            <thead class="bg-gray-100 dark:bg-gray-700">
            <tr>
                <th scope="col" class="w-10 p-3 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400"></th>
                <th scope="col" class="p-3 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400">Name</th>
                <th scope="col" class="p-3 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400">BIC / BLZ</th>
                <th scope="col" class="p-3 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400">HBCI</th>
                <th scope="col" class="w-16 p-3 text-xs font-medium text-center text-gray-500 uppercase dark:text-gray-400">Logo</th>
                <th scope="col" class="p-3 text-xs font-medium text-center text-gray-500 uppercase dark:text-gray-400">Colors</th>
                <th scope="col" class="p-3 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400"></th>
            </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200 dark:bg-gray-800 dark:divide-gray-700">
            {% for account in accounts %}
                <tr class="hover:bg-gray-100 dark:hover:bg-gray-700">
                    <td class="p-3 text-xs font-normal text-gray-500 whitespace-nowrap dark:text-gray-400">
                        {% if account.subAccounts.count > 0 %}
                            <a href="#" data-action="click->account-table#toggle" data-account-table-target-param="account-{{ account.id }}">
                            <svg class="size-4 text-gray-500 dark:text-white" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24">
                                <path class="subaccounts-expanded:hidden block" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m19 9-7 7-7-7"/>
                                <path class="subaccounts-expanded:block hidden" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m5 15 7-7 7 7"/>
                            </svg>
                            </a>
                        {% endif %}
                    </td>
                    <td class="p-3 text-xs font-normal text-gray-500 whitespace-nowrap dark:text-gray-400">{{ account.name }}</td>
                    <td class="p-3 text-xs font-normal text-gray-500 whitespace-nowrap dark:text-gray-400">{{ account.bic }} / {{ account.bankCode }}</td>
                    <td class="p-3 text-xs font-normal text-gray-500 whitespace-nowrap dark:text-gray-400">{{ account.url }}</td>
                    <td class="p-3 text-xs font-normal text-gray-500 whitespace-nowrap dark:text-gray-400 text-center">
                        {% if account.logo %}
                            <img src="{{ asset('uploads/logos/'~account.logo) }}" class="max-h-8 max-w-16 inline-block" />
                        {% endif %}
                    </td>
                    <td class="p-3 text-xs font-normal text-gray-500 whitespace-nowrap dark:text-gray-400 text-center">
                        <div class="inline-block size-6 border" style="background-color: {{ account.foregroundColor }}"></div>
                        <div class="inline-block size-6 border" style="background-color: {{ account.backgroundColor }}"></div>
                    </td>
                    <td class="p-3 pe-6 space-x-2 text-gray-500 whitespace-nowrap text-right">
{#                        <a#}
{#                            href="{{ path('app_settings_account_step1', {'id': account.id}) }}"#}
{#                        >#}
{#                            <i class="fa-solid fa-square-pen text-lg hover:text-gray-900"></i>#}
{#                        </a>#}

                        <a href="{{ path('app_settings_account_edit_credentials', {'id': account.id}) }}" title="Edit credentials" data-turbo-frame="mdl"><i class="fa-solid fa-key"></i></a>
                        <a href="{{ path('app_settings_account_edit_tan', {'id': account.id}) }}" title="Edit TAN Mode" data-turbo-frame="mdl"><i class="fa-regular fa-credit-card"></i></a>
                        <a href="{{ path('app_settings_account_edit_basic', {'id': account.id}) }}" title="Edit" data-turbo-frame="mdl"><i class="fa-solid fa-pen"></i></a>
                    </td>
                </tr>
                {% if account.subAccounts.count > 0 %}
                    <tr id="account-{{ account.id }}" class="{% if editedAccountId != 0 and editedAccountId == account.id %}{% else %}hidden{% endif %}">
                        <td class="p-3 text-xs font-normal text-gray-500 whitespace-nowrap dark:text-gray-400"></td>
                        <td class="p-3" colspan="6">
                            <h5 class="mb-3">Subaccounts</h5>
                            <div class="flex flex-row flex-wrap gap-4">
                                {% for subAccount in account.subAccounts %}
                                    <a
                                        href="{{ path('app_settings_subaccount_edit', {'id': subAccount.id}) }}"
                                        data-turbo-frame="mdl"
                                        class="block group relative text-xs font-normal p-4 border rounded-lg text-gray-600 dark:text-gray-400 bg-gray-50 space-y-1"
                                    >
                                        <i class="absolute invisible group-hover:visible top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 text-gray-600 text-2xl fa-solid fa-pen"></i>
                                        <div class="group-hover:opacity-50">
                                            {% if subAccount.description is empty %}
                                            <p class="text-gray-500/50"><i>No description</i></p>
                                            {% else %}
                                            <p><b>{{ subAccount.description }}</b></p>
                                            {% endif %}
                                            <div class="flex flex-row">
                                                <div class="text-gray-500/50 mr-4">IBAN</div>
                                                <div class="grow flex justify-end"><b>{{ subAccount.iban }}</b></div>
                                            </div>
                                            <div class="flex flex-row">
                                                <div class="text-gray-500/50 mr-4">Account Number</div>
                                                <div class="grow flex justify-end"><b>{{ subAccount.accountNumber }}</b></div>
                                            </div>
                                            <div class="flex flex-row">
                                                <div class="text-gray-500/50 mr-4">Enabled</div>
                                                <div class="grow flex justify-end">
                                                    {% if subAccount.enabled %}
                                                        <i class="text-emerald-700 fa-solid fa-circle-check"></i>
                                                    {% else %}
                                                        <i class="text-red-700 fa-solid fa-circle-xmark"></i>
                                                    {% endif %}
                                                </div>
                                            </div>
                                        </div>
                                    </a>
                                {% endfor %}
                            </div>
                        </td>
                    </tr>
                {% endif %}
            {% else %}
                <tr>
                    <td colspan="7" class="p-4 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400">No records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </twig:Content>
{% endblock %}
